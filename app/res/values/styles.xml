<resources>
    <!-- Fond sombre pendant le démarrage (aucun écran blanc) -->
    <style name="AppTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0B0F1E</item>
        <item name="android:statusBarColor">#0B0F1E</item>
        <item name="android:navigationBarColor">#0B0F1E</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
